import { WithId } from '../../lib/utils';

export interface Post extends WithId {
    by: string;
    descendants: number;
    id: number;
    kids: number[];
    score: number;
    time: number;
    title: string;
    type: string;
    url: string;
}